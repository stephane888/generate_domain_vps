name: generate domain vps
type: module
description: Allows you to generate in vhost locally, on a VPS or multi-hosting (Limit to OVH for the moment)
package: Custom
core_version_requirement: ^9 || ^10
dependencies:
  - ovh_api_rest:ovh_api_rest
